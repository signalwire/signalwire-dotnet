/*
 * Copyright (c) 2025 SignalWire
 *
 * Licensed under the MIT License.
 * See LICENSE file in the project root for full license information.
 */
// <auto-generated>
// Code generated by scripts/generate_rest_tests.py; DO NOT EDIT.
//
// AUTO-GENERATED full-mock REST wire tests for the 'projects' namespace — regenerate:
//   python3 scripts/generate_rest_tests.py
//
// Each route the GENERATED client implements (captured from the real client by
// tools/RestTestPlan via scripts/rest-test-plan.sh, joined to the spec
// operationId) gets a SUCCESS test (call it, assert Method + MatchedRoute on the
// mock journal) and an ERROR test (arm a 500, assert SignalWireRestError with
// StatusCode == 500 + journal route/status). The assertion oracle is the spec
// operationId — independent of the resource generator — so these catch
// SDK-vs-contract drift, not a generator self-snapshot. Shared full-mock harness
// fixtures (MockServerFixture); routes drive the generated ResourceTree.
// </auto-generated>
#nullable enable

using System.Collections.Generic;
using System.Threading.Tasks;
using SignalWire.REST;
using SignalWire.REST.Namespaces.Generated;
using SignalWire.Tests.Mock;
using Xunit;

namespace SignalWire.Tests.RestMock.Generated;

/// <summary>Generated full-mock REST wire tests for the 'projects' namespace.</summary>
// [Collection] serializes all RestCoverage classes onto one xUnit collection:
// they share one mock + one journal and each resets it in its ctor, so xUnit's
// default per-class parallelism lets classes wipe each other's recorded route
// hits (intermittent "route not covered"). xUnit reads [Collection] with
// inherit:false, so it must sit on the CONCRETE class, not only CoverageBase.
[Collection("RestCoverage")]
[Trait("Category", "RestCoverage")]
public class ProjectsGeneratedTest : CoverageBase
{
    public ProjectsGeneratedTest(MockServerFixture fixture) : base(fixture) { }

    private ResourceTree NewTree() => new(NewHttp());

    [Fact]
    public async Task Projects_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Projects.CreateAsync(new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("projects.create_subproject", j.MatchedRoute);
    }

    [Fact]
    public async Task Projects_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("projects.create_subproject", 500,
            () => tree.Projects.CreateAsync(new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Projects_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Projects.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("projects.delete_subproject", j.MatchedRoute);
    }

    [Fact]
    public async Task Projects_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("projects.delete_subproject", 500,
            () => tree.Projects.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Projects_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Projects.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("projects.get_project", j.MatchedRoute);
    }

    [Fact]
    public async Task Projects_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("projects.get_project", 500,
            () => tree.Projects.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Projects_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Projects.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("projects.list_projects", j.MatchedRoute);
    }

    [Fact]
    public async Task Projects_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("projects.list_projects", 500,
            () => tree.Projects.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Projects_RotateSigningKey_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Projects.RotateSigningKeyAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("projects.rotate_signing_key", j.MatchedRoute);
    }

    [Fact]
    public async Task Projects_RotateSigningKey_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("projects.rotate_signing_key", 500,
            () => tree.Projects.RotateSigningKeyAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Projects_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Projects.UpdateAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PATCH", j.Method);
        Assert.Equal("projects.update_project", j.MatchedRoute);
    }

    [Fact]
    public async Task Projects_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("projects.update_project", 500,
            () => tree.Projects.UpdateAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }
}
