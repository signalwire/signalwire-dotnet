/*
 * Copyright (c) 2025 SignalWire
 *
 * Licensed under the MIT License.
 * See LICENSE file in the project root for full license information.
 */
// <auto-generated>
// Code generated by scripts/generate_rest_tests.py; DO NOT EDIT.
//
// AUTO-GENERATED full-mock REST wire tests for the 'calling' namespace — regenerate:
//   python3 scripts/generate_rest_tests.py
//
// Each route the GENERATED client implements (captured from the real client by
// tools/RestTestPlan via scripts/rest-test-plan.sh, joined to the spec
// operationId) gets a SUCCESS test (call it, assert Method + MatchedRoute on the
// mock journal) and an ERROR test (arm a 500, assert SignalWireRestError with
// StatusCode == 500 + journal route/status). The assertion oracle is the spec
// operationId — independent of the resource generator — so these catch
// SDK-vs-contract drift, not a generator self-snapshot. Shared full-mock harness
// fixtures (MockServerFixture); routes drive the generated ResourceTree.
// </auto-generated>
#nullable enable

using System.Collections.Generic;
using System.Threading.Tasks;
using SignalWire.REST;
using SignalWire.REST.Namespaces.Generated;
using SignalWire.Tests.Mock;
using Xunit;

namespace SignalWire.Tests.RestMock.Generated;

/// <summary>Generated full-mock REST wire tests for the 'calling' namespace.</summary>
// [Collection] serializes all RestCoverage classes onto one xUnit collection:
// they share one mock + one journal and each resets it in its ctor, so xUnit's
// default per-class parallelism lets classes wipe each other's recorded route
// hits (intermittent "route not covered"). xUnit reads [Collection] with
// inherit:false, so it must sit on the CONCRETE class, not only CoverageBase.
[Collection("RestCoverage")]
[Trait("Category", "RestCoverage")]
public class CallingGeneratedTest : CoverageBase
{
    public CallingGeneratedTest(MockServerFixture fixture) : base(fixture) { }

    private ResourceTree NewTree() => new(NewHttp());

    [Fact]
    public async Task Calling_AiHold_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.AiHoldAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_AiHold_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.AiHoldAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_AiMessage_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.AiMessageAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_AiMessage_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.AiMessageAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_AiStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.AiStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_AiStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.AiStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_AiUnhold_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.AiUnholdAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_AiUnhold_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.AiUnholdAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Collect_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.CollectAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Collect_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.CollectAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_CollectStartInputTimers_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.CollectStartInputTimersAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_CollectStartInputTimers_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.CollectStartInputTimersAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_CollectStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.CollectStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_CollectStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.CollectStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Denoise_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.DenoiseAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Denoise_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.DenoiseAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_DenoiseStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.DenoiseStopAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_DenoiseStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.DenoiseStopAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Detect_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.DetectAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Detect_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.DetectAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_DetectStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.DetectStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_DetectStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.DetectStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Dial_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.DialAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Dial_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.DialAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Disconnect_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.DisconnectAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Disconnect_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.DisconnectAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_End_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.EndAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_End_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.EndAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_LiveTranscribe_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.LiveTranscribeAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_LiveTranscribe_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.LiveTranscribeAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_LiveTranslate_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.LiveTranslateAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_LiveTranslate_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.LiveTranslateAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Play_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.PlayAsync("x", new List<object>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Play_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.PlayAsync("x", new List<object>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_PlayPause_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.PlayPauseAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_PlayPause_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.PlayPauseAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_PlayResume_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.PlayResumeAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_PlayResume_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.PlayResumeAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_PlayStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.PlayStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_PlayStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.PlayStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_PlayVolume_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.PlayVolumeAsync("x", "x", 1.0);
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_PlayVolume_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.PlayVolumeAsync("x", "x", 1.0));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_ReceiveFaxStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.ReceiveFaxStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_ReceiveFaxStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.ReceiveFaxStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Record_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.RecordAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Record_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.RecordAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_RecordPause_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.RecordPauseAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_RecordPause_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.RecordPauseAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_RecordResume_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.RecordResumeAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_RecordResume_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.RecordResumeAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_RecordStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.RecordStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_RecordStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.RecordStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Refer_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.ReferAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Refer_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.ReferAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_SendFaxStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.SendFaxStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_SendFaxStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.SendFaxStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Stream_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.StreamAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Stream_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.StreamAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_StreamStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.StreamStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_StreamStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.StreamStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Tap_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.TapAsync("x", new Dictionary<string, object?>(), new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Tap_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.TapAsync("x", new Dictionary<string, object?>(), new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_TapStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.TapStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_TapStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.TapStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Transcribe_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.TranscribeAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Transcribe_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.TranscribeAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_TranscribeStop_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.TranscribeStopAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_TranscribeStop_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.TranscribeStopAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Transfer_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.TransferAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Transfer_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.TransferAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.UpdateAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.UpdateAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Calling_UserEvent_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Calling.UserEventAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("calling.call-commands", j.MatchedRoute);
    }

    [Fact]
    public async Task Calling_UserEvent_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("calling.call-commands", 500,
            () => tree.Calling.UserEventAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }
}
