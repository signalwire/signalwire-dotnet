/*
 * Copyright (c) 2025 SignalWire
 *
 * Licensed under the MIT License.
 * See LICENSE file in the project root for full license information.
 */
// <auto-generated>
// Code generated by scripts/generate_rest_tests.py; DO NOT EDIT.
//
// AUTO-GENERATED full-mock REST wire tests for the 'video' namespace — regenerate:
//   python3 scripts/generate_rest_tests.py
//
// Each route the GENERATED client implements (captured from the real client by
// tools/RestTestPlan via scripts/rest-test-plan.sh, joined to the spec
// operationId) gets a SUCCESS test (call it, assert Method + MatchedRoute on the
// mock journal) and an ERROR test (arm a 500, assert SignalWireRestError with
// StatusCode == 500 + journal route/status). The assertion oracle is the spec
// operationId — independent of the resource generator — so these catch
// SDK-vs-contract drift, not a generator self-snapshot. Shared full-mock harness
// fixtures (MockServerFixture); routes drive the generated ResourceTree.
// </auto-generated>
#nullable enable

using System.Collections.Generic;
using System.Threading.Tasks;
using SignalWire.REST;
using SignalWire.REST.Namespaces.Generated;
using SignalWire.Tests.Mock;
using Xunit;

namespace SignalWire.Tests.RestMock.Generated;

/// <summary>Generated full-mock REST wire tests for the 'video' namespace.</summary>
[Trait("Category", "RestCoverage")]
public class VideoGeneratedTest : CoverageBase
{
    public VideoGeneratedTest(MockServerFixture fixture) : base(fixture) { }

    private ResourceTree NewTree() => new(NewHttp());

    [Fact]
    public async Task VideoConferenceTokens_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.ConferenceTokens.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.get_conference_token", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferenceTokens_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.get_conference_token", 500,
            () => tree.Video.ConferenceTokens.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoConferenceTokens_Reset_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.ConferenceTokens.ResetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("video.reset_conference_token", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferenceTokens_Reset_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.reset_conference_token", 500,
            () => tree.Video.ConferenceTokens.ResetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoConferences_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Conferences.CreateAsync(new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("video.create_video_conference", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferences_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.create_video_conference", 500,
            () => tree.Video.Conferences.CreateAsync(new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoConferences_CreateStream_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Conferences.CreateStreamAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("video.create_conference_stream", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferences_CreateStream_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.create_conference_stream", 500,
            () => tree.Video.Conferences.CreateStreamAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoConferences_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Conferences.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("video.delete_video_conference", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferences_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.delete_video_conference", 500,
            () => tree.Video.Conferences.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoConferences_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Conferences.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.get_video_conference", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferences_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.get_video_conference", 500,
            () => tree.Video.Conferences.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoConferences_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Conferences.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_video_conferences", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferences_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_video_conferences", 500,
            () => tree.Video.Conferences.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoConferences_ListConferenceTokens_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Conferences.ListConferenceTokensAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_conference_tokens", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferences_ListConferenceTokens_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_conference_tokens", 500,
            () => tree.Video.Conferences.ListConferenceTokensAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoConferences_ListStreams_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Conferences.ListStreamsAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_conference_streams", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferences_ListStreams_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_conference_streams", 500,
            () => tree.Video.Conferences.ListStreamsAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoConferences_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Conferences.UpdateAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("video.update_video_conference", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoConferences_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.update_video_conference", 500,
            () => tree.Video.Conferences.UpdateAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomRecordings_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomRecordings.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("video.delete_room_recording", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomRecordings_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.delete_room_recording", 500,
            () => tree.Video.RoomRecordings.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomRecordings_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomRecordings.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.get_room_recording", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomRecordings_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.get_room_recording", 500,
            () => tree.Video.RoomRecordings.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomRecordings_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomRecordings.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_room_recordings", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomRecordings_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_room_recordings", 500,
            () => tree.Video.RoomRecordings.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomRecordings_ListEvents_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomRecordings.ListEventsAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_room_recording_events", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomRecordings_ListEvents_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_room_recording_events", 500,
            () => tree.Video.RoomRecordings.ListEventsAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomSessions_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomSessions.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.get_room_session", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomSessions_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.get_room_session", 500,
            () => tree.Video.RoomSessions.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomSessions_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomSessions.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_room_sessions", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomSessions_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_room_sessions", 500,
            () => tree.Video.RoomSessions.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomSessions_ListEvents_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomSessions.ListEventsAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_room_session_events", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomSessions_ListEvents_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_room_session_events", 500,
            () => tree.Video.RoomSessions.ListEventsAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomSessions_ListMembers_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomSessions.ListMembersAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_room_session_members", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomSessions_ListMembers_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_room_session_members", 500,
            () => tree.Video.RoomSessions.ListMembersAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomSessions_ListRecordings_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomSessions.ListRecordingsAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_room_session_recordings", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomSessions_ListRecordings_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_room_session_recordings", 500,
            () => tree.Video.RoomSessions.ListRecordingsAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRoomTokens_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.RoomTokens.CreateAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("video.create_room_token", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRoomTokens_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.create_room_token", 500,
            () => tree.Video.RoomTokens.CreateAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRooms_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Rooms.CreateAsync(new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("video.create_room", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRooms_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.create_room", 500,
            () => tree.Video.Rooms.CreateAsync(new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRooms_CreateStream_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Rooms.CreateStreamAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("video.create_room_stream", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRooms_CreateStream_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.create_room_stream", 500,
            () => tree.Video.Rooms.CreateStreamAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRooms_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Rooms.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("video.delete_room", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRooms_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.delete_room", 500,
            () => tree.Video.Rooms.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRooms_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Rooms.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.get_room_by_name", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRooms_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.get_room_by_name", 500,
            () => tree.Video.Rooms.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRooms_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Rooms.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_rooms", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRooms_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_rooms", 500,
            () => tree.Video.Rooms.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRooms_ListStreams_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Rooms.ListStreamsAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.list_room_streams", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRooms_ListStreams_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.list_room_streams", 500,
            () => tree.Video.Rooms.ListStreamsAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoRooms_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Rooms.UpdateAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("video.update_room", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoRooms_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.update_room", 500,
            () => tree.Video.Rooms.UpdateAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoStreams_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Streams.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("video.delete_stream", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoStreams_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.delete_stream", 500,
            () => tree.Video.Streams.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoStreams_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Streams.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("video.get_stream", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoStreams_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.get_stream", 500,
            () => tree.Video.Streams.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VideoStreams_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Video.Streams.UpdateAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("video.update_stream", j.MatchedRoute);
    }

    [Fact]
    public async Task VideoStreams_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("video.update_stream", 500,
            () => tree.Video.Streams.UpdateAsync("x", "x"));
        Assert.Equal(500, status);
    }
}
