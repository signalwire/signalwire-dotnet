/*
 * Copyright (c) 2025 SignalWire
 *
 * Licensed under the MIT License.
 * See LICENSE file in the project root for full license information.
 */
// <auto-generated>
// Code generated by scripts/generate_rest_tests.py; DO NOT EDIT.
//
// AUTO-GENERATED full-mock REST wire tests for the 'relay-rest' namespace — regenerate:
//   python3 scripts/generate_rest_tests.py
//
// Each route the GENERATED client implements (captured from the real client by
// tools/RestTestPlan via scripts/rest-test-plan.sh, joined to the spec
// operationId) gets a SUCCESS test (call it, assert Method + MatchedRoute on the
// mock journal) and an ERROR test (arm a 500, assert SignalWireRestError with
// StatusCode == 500 + journal route/status). The assertion oracle is the spec
// operationId — independent of the resource generator — so these catch
// SDK-vs-contract drift, not a generator self-snapshot. Shared full-mock harness
// fixtures (MockServerFixture); routes drive the generated ResourceTree.
// </auto-generated>
#nullable enable

using System.Collections.Generic;
using System.Threading.Tasks;
using SignalWire.REST;
using SignalWire.REST.Namespaces.Generated;
using SignalWire.Tests.Mock;
using Xunit;

namespace SignalWire.Tests.RestMock.Generated;

/// <summary>Generated full-mock REST wire tests for the 'relay-rest' namespace.</summary>
[Trait("Category", "RestCoverage")]
public class RelayRestGeneratedTest : CoverageBase
{
    public RelayRestGeneratedTest(MockServerFixture fixture) : base(fixture) { }

    private ResourceTree NewTree() => new(NewHttp());

    [Fact]
    public async Task Addresses_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Addresses.CreateAsync("x", "x", "x", "x", "x", "x", "x", "x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.create_address", j.MatchedRoute);
    }

    [Fact]
    public async Task Addresses_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.create_address", 500,
            () => tree.Addresses.CreateAsync("x", "x", "x", "x", "x", "x", "x", "x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Addresses_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Addresses.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("relay-rest.delete_address", j.MatchedRoute);
    }

    [Fact]
    public async Task Addresses_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.delete_address", 500,
            () => tree.Addresses.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Addresses_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Addresses.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.get_address", j.MatchedRoute);
    }

    [Fact]
    public async Task Addresses_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.get_address", 500,
            () => tree.Addresses.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Addresses_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Addresses.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_addresses", j.MatchedRoute);
    }

    [Fact]
    public async Task Addresses_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_addresses", 500,
            () => tree.Addresses.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task ImportedNumbers_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.ImportedNumbers.CreateAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.create_imported_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task ImportedNumbers_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.create_imported_phone_number", 500,
            () => tree.ImportedNumbers.CreateAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Lookup_PhoneNumber_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Lookup.PhoneNumberAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.lookup_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task Lookup_PhoneNumber_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.lookup_phone_number", 500,
            () => tree.Lookup.PhoneNumberAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Mfa_Call_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Mfa.CallAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.request_mfa_call", j.MatchedRoute);
    }

    [Fact]
    public async Task Mfa_Call_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.request_mfa_call", 500,
            () => tree.Mfa.CallAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Mfa_Sms_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Mfa.SmsAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.request_mfa_sms", j.MatchedRoute);
    }

    [Fact]
    public async Task Mfa_Sms_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.request_mfa_sms", 500,
            () => tree.Mfa.SmsAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Mfa_Verify_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Mfa.VerifyAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.verify_mfa_token", j.MatchedRoute);
    }

    [Fact]
    public async Task Mfa_Verify_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.verify_mfa_token", 500,
            () => tree.Mfa.VerifyAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task NumberGroups_AddMembership_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.NumberGroups.AddMembershipAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.create_number_group_membership", j.MatchedRoute);
    }

    [Fact]
    public async Task NumberGroups_AddMembership_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.create_number_group_membership", 500,
            () => tree.NumberGroups.AddMembershipAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task NumberGroups_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.NumberGroups.CreateAsync(new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.create_number_group", j.MatchedRoute);
    }

    [Fact]
    public async Task NumberGroups_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.create_number_group", 500,
            () => tree.NumberGroups.CreateAsync(new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task NumberGroups_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.NumberGroups.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("relay-rest.delete_number_group", j.MatchedRoute);
    }

    [Fact]
    public async Task NumberGroups_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.delete_number_group", 500,
            () => tree.NumberGroups.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task NumberGroups_DeleteMembership_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.NumberGroups.DeleteMembershipAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("relay-rest.delete_number_group_membership", j.MatchedRoute);
    }

    [Fact]
    public async Task NumberGroups_DeleteMembership_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.delete_number_group_membership", 500,
            () => tree.NumberGroups.DeleteMembershipAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task NumberGroups_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.NumberGroups.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_number_group", j.MatchedRoute);
    }

    [Fact]
    public async Task NumberGroups_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_number_group", 500,
            () => tree.NumberGroups.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task NumberGroups_GetMembership_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.NumberGroups.GetMembershipAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_number_group_membership", j.MatchedRoute);
    }

    [Fact]
    public async Task NumberGroups_GetMembership_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_number_group_membership", 500,
            () => tree.NumberGroups.GetMembershipAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task NumberGroups_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.NumberGroups.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_number_groups", j.MatchedRoute);
    }

    [Fact]
    public async Task NumberGroups_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_number_groups", 500,
            () => tree.NumberGroups.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task NumberGroups_ListMemberships_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.NumberGroups.ListMembershipsAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_number_group_memberships", j.MatchedRoute);
    }

    [Fact]
    public async Task NumberGroups_ListMemberships_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_number_group_memberships", 500,
            () => tree.NumberGroups.ListMembershipsAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task NumberGroups_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.NumberGroups.UpdateAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_number_group", j.MatchedRoute);
    }

    [Fact]
    public async Task NumberGroups_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_number_group", 500,
            () => tree.NumberGroups.UpdateAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.CreateAsync(new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.purchase_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.purchase_phone_number", 500,
            () => tree.PhoneNumbers.CreateAsync(new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("relay-rest.release_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.release_phone_number", 500,
            () => tree.PhoneNumbers.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_phone_number", 500,
            () => tree.PhoneNumbers.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_phone_numbers", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_phone_numbers", 500,
            () => tree.PhoneNumbers.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_Search_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.SearchAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.search_available_phone_numbers", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_Search_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.search_available_phone_numbers", 500,
            () => tree.PhoneNumbers.SearchAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_SetAiAgent_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.SetAiAgentAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_SetAiAgent_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_phone_number", 500,
            () => tree.PhoneNumbers.SetAiAgentAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_SetCallFlow_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.SetCallFlowAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_SetCallFlow_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_phone_number", 500,
            () => tree.PhoneNumbers.SetCallFlowAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_SetCxmlApplication_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.SetCxmlApplicationAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_SetCxmlApplication_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_phone_number", 500,
            () => tree.PhoneNumbers.SetCxmlApplicationAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_SetCxmlWebhook_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.SetCxmlWebhookAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_SetCxmlWebhook_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_phone_number", 500,
            () => tree.PhoneNumbers.SetCxmlWebhookAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_SetRelayApplication_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.SetRelayApplicationAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_SetRelayApplication_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_phone_number", 500,
            () => tree.PhoneNumbers.SetRelayApplicationAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_SetRelayTopic_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.SetRelayTopicAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_SetRelayTopic_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_phone_number", 500,
            () => tree.PhoneNumbers.SetRelayTopicAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_SetSwmlWebhook_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.SetSwmlWebhookAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_SetSwmlWebhook_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_phone_number", 500,
            () => tree.PhoneNumbers.SetSwmlWebhookAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task PhoneNumbers_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.PhoneNumbers.UpdateAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_phone_number", j.MatchedRoute);
    }

    [Fact]
    public async Task PhoneNumbers_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_phone_number", 500,
            () => tree.PhoneNumbers.UpdateAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Queues_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Queues.CreateAsync(new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.create_queue", j.MatchedRoute);
    }

    [Fact]
    public async Task Queues_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.create_queue", 500,
            () => tree.Queues.CreateAsync(new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Queues_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Queues.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("relay-rest.delete_queue", j.MatchedRoute);
    }

    [Fact]
    public async Task Queues_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.delete_queue", 500,
            () => tree.Queues.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Queues_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Queues.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.get_queue", j.MatchedRoute);
    }

    [Fact]
    public async Task Queues_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.get_queue", 500,
            () => tree.Queues.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Queues_GetMember_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Queues.GetMemberAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_queue_member", j.MatchedRoute);
    }

    [Fact]
    public async Task Queues_GetMember_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_queue_member", 500,
            () => tree.Queues.GetMemberAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Queues_GetNextMember_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Queues.GetNextMemberAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_next_queue_member", j.MatchedRoute);
    }

    [Fact]
    public async Task Queues_GetNextMember_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_next_queue_member", 500,
            () => tree.Queues.GetNextMemberAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Queues_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Queues.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_queues", j.MatchedRoute);
    }

    [Fact]
    public async Task Queues_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_queues", 500,
            () => tree.Queues.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Queues_ListMembers_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Queues.ListMembersAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_queue_members", j.MatchedRoute);
    }

    [Fact]
    public async Task Queues_ListMembers_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_queue_members", 500,
            () => tree.Queues.ListMembersAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Queues_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Queues.UpdateAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_queue", j.MatchedRoute);
    }

    [Fact]
    public async Task Queues_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_queue", 500,
            () => tree.Queues.UpdateAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Recordings_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Recordings.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("relay-rest.delete_recording", j.MatchedRoute);
    }

    [Fact]
    public async Task Recordings_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.delete_recording", 500,
            () => tree.Recordings.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Recordings_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Recordings.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.get_recording", j.MatchedRoute);
    }

    [Fact]
    public async Task Recordings_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.get_recording", 500,
            () => tree.Recordings.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task Recordings_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Recordings.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_recordings", j.MatchedRoute);
    }

    [Fact]
    public async Task Recordings_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_recordings", 500,
            () => tree.Recordings.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryBrands_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Brands.CreateAsync(new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.create_brand", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryBrands_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.create_brand", 500,
            () => tree.Registry.Brands.CreateAsync(new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryBrands_CreateCampaign_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Brands.CreateCampaignAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.create_campaign", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryBrands_CreateCampaign_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.create_campaign", 500,
            () => tree.Registry.Brands.CreateCampaignAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryBrands_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Brands.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_brand", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryBrands_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_brand", 500,
            () => tree.Registry.Brands.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryBrands_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Brands.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_brands", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryBrands_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_brands", 500,
            () => tree.Registry.Brands.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryBrands_ListCampaigns_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Brands.ListCampaignsAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_campaigns", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryBrands_ListCampaigns_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_campaigns", 500,
            () => tree.Registry.Brands.ListCampaignsAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryCampaigns_CreateOrder_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Campaigns.CreateOrderAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.create_order", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryCampaigns_CreateOrder_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.create_order", 500,
            () => tree.Registry.Campaigns.CreateOrderAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryCampaigns_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Campaigns.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_campaign", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryCampaigns_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_campaign", 500,
            () => tree.Registry.Campaigns.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryCampaigns_ListNumbers_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Campaigns.ListNumbersAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_number_assignments", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryCampaigns_ListNumbers_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_number_assignments", 500,
            () => tree.Registry.Campaigns.ListNumbersAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryCampaigns_ListOrders_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Campaigns.ListOrdersAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_orders", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryCampaigns_ListOrders_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_orders", 500,
            () => tree.Registry.Campaigns.ListOrdersAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryCampaigns_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Campaigns.UpdateAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_campaign", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryCampaigns_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_campaign", 500,
            () => tree.Registry.Campaigns.UpdateAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryNumbers_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Numbers.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("relay-rest.delete_number_assignment", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryNumbers_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.delete_number_assignment", 500,
            () => tree.Registry.Numbers.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task RegistryOrders_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.Registry.Orders.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_order", j.MatchedRoute);
    }

    [Fact]
    public async Task RegistryOrders_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_order", 500,
            () => tree.Registry.Orders.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task ShortCodes_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.ShortCodes.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_short_code", j.MatchedRoute);
    }

    [Fact]
    public async Task ShortCodes_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_short_code", 500,
            () => tree.ShortCodes.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task ShortCodes_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.ShortCodes.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_short_codes", j.MatchedRoute);
    }

    [Fact]
    public async Task ShortCodes_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_short_codes", 500,
            () => tree.ShortCodes.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task ShortCodes_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.ShortCodes.UpdateAsync("x", "x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_short_code", j.MatchedRoute);
    }

    [Fact]
    public async Task ShortCodes_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_short_code", 500,
            () => tree.ShortCodes.UpdateAsync("x", "x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task SipProfile_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.SipProfile.GetAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_sip_profile", j.MatchedRoute);
    }

    [Fact]
    public async Task SipProfile_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_sip_profile", 500,
            () => tree.SipProfile.GetAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task SipProfile_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.SipProfile.UpdateAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_sip_profile", j.MatchedRoute);
    }

    [Fact]
    public async Task SipProfile_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_sip_profile", 500,
            () => tree.SipProfile.UpdateAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VerifiedCallers_Create_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.VerifiedCallers.CreateAsync(new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.create_verified_caller_id", j.MatchedRoute);
    }

    [Fact]
    public async Task VerifiedCallers_Create_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.create_verified_caller_id", 500,
            () => tree.VerifiedCallers.CreateAsync(new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VerifiedCallers_Delete_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.VerifiedCallers.DeleteAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("DELETE", j.Method);
        Assert.Equal("relay-rest.delete_verified_caller_id", j.MatchedRoute);
    }

    [Fact]
    public async Task VerifiedCallers_Delete_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.delete_verified_caller_id", 500,
            () => tree.VerifiedCallers.DeleteAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VerifiedCallers_Get_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.VerifiedCallers.GetAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.retrieve_verified_caller_id", j.MatchedRoute);
    }

    [Fact]
    public async Task VerifiedCallers_Get_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.retrieve_verified_caller_id", 500,
            () => tree.VerifiedCallers.GetAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VerifiedCallers_List_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.VerifiedCallers.ListAsync();
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("GET", j.Method);
        Assert.Equal("relay-rest.list_verified_caller_ids", j.MatchedRoute);
    }

    [Fact]
    public async Task VerifiedCallers_List_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.list_verified_caller_ids", 500,
            () => tree.VerifiedCallers.ListAsync());
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VerifiedCallers_RedialVerification_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.VerifiedCallers.RedialVerificationAsync("x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("POST", j.Method);
        Assert.Equal("relay-rest.redial_verification_call", j.MatchedRoute);
    }

    [Fact]
    public async Task VerifiedCallers_RedialVerification_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.redial_verification_call", 500,
            () => tree.VerifiedCallers.RedialVerificationAsync("x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VerifiedCallers_SubmitVerification_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.VerifiedCallers.SubmitVerificationAsync("x", "x");
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.validate_verification_code", j.MatchedRoute);
    }

    [Fact]
    public async Task VerifiedCallers_SubmitVerification_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.validate_verification_code", 500,
            () => tree.VerifiedCallers.SubmitVerificationAsync("x", "x"));
        Assert.Equal(500, status);
    }

    [Fact]
    public async Task VerifiedCallers_Update_Success()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var body = await tree.VerifiedCallers.UpdateAsync("x", new Dictionary<string, object?>());
        Assert.NotNull(body);
        var j = Fixture.Harness.Journal.Last();
        Assert.Equal("PUT", j.Method);
        Assert.Equal("relay-rest.update_verified_caller_id", j.MatchedRoute);
    }

    [Fact]
    public async Task VerifiedCallers_Update_Error()
    {
        if (!Fixture.Available) return;
        var tree = NewTree();
        var status = await AssertErrorAsync("relay-rest.update_verified_caller_id", 500,
            () => tree.VerifiedCallers.UpdateAsync("x", new Dictionary<string, object?>()));
        Assert.Equal(500, status);
    }
}
